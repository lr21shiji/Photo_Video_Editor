<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_margin="10dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/item_image_view_image"
        android:src="@drawable/dieu_anh_biet"
        android:layout_width="60dp"
        android:layout_height="60dp"/>

    <TextView
        android:text="Folder"
        android:textSize="18sp"
        android:textColor="@color/black"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@id/item_image_view_image"
        android:id="@+id/item_text_view_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:layout_below="@id/item_text_view_name"
        android:text="Folder"
        android:textSize="14sp"
        android:textColor="@color/black"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@id/item_image_view_image"
        android:id="@+id/item_text_view_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</RelativeLayout>